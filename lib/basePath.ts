/**
 * Nom du dépôt GitHub = sous-chemin du site sur GitHub Pages. Seul endroit
 * où cette chaîne existe dans le projet — repris à la fois par
 * `next.config.ts` (basePath) et par `lib/assetPath.ts`.
 */
export const BASE_PATH = "/birthday-sensei";
